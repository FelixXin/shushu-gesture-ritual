name: shushu-gesture-ritual

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"

  web:
    build: ./frontend
    environment:
      API_PROXY_TARGET: http://api:8000
    ports:
      - "5173:5173"
    depends_on:
      - api
